#include "TripleSys.h"
int check1(char result[][nPlayers], int nlines)
{
	// return 1 - continue, 0 - stop (calculate new matrix)
	return 1; 
}
int compareMatrix(char result[][nPlayers], int nlines, char* transition)
{
	// return 1 - new result greater, 0 - the same, 1 - less
	return 1;
}